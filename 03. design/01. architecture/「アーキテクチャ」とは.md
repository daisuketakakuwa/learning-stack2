# いろんなレイヤーに「アーキテクチャ」が存在する
```
System Architecture
  ├─ Application Architecture
  │   ├─ Frontend
  │   │   └─ Frontend Software Architecture
  │   ├─ Backend
  │   │   └─ Backend Software Architecture
  │   └─ Database (application layer)
  └─ Infrastructure Architecture
      ├─ Frontend runtime
      ├─ Backend runtime
      ├─ Database (infra layer)
      └─ CDN / Load Balancer / etc.
```

1. システム = 「アプリ」＋「インフラ」を含めたアーキテクチャ
2. アプリ   = 「FEアプリ」＋「BEアプリ」＋「DB(アプリレイヤー)」を含めたアーキテクチャ
3. ソフト   = FE、BEそれぞれに閉じたソフトに関するアーキテクチャ

## システムアーキテクチャ
```
┌─────────────────────────────┐
│ User / Client               │ ← ブラウザ・モバイル
├─────────────────────────────┤
│ CDN / Load Balancer         │ ← リクエスト分散、キャッシュ
├─────────────────────────────┤
│ Web / Application Servers   │ ← BEのSWAが稼働するサーバ群
├─────────────────────────────┤
│ Database Cluster / Cache    │ ← 永続化・高速アクセス
├─────────────────────────────┤
│ Message Queue / Event Bus   │ ← 非同期処理・連携
├─────────────────────────────┤
│ External Systems / APIs     │ ← 外部サービス、他システム
└─────────────────────────────┘
```


## アプリケーションアーキテクチャ
```
┌─────────────────────────┐
│ Frontend / Client       │ ← ユーザーUI、ブラウザやモバイル
├─────────────────────────┤
│ Backend / API Server    │ ← BEロジック(SWAがここで動作)
├─────────────────────────┤
│ Database / Cache        │ ← 永続化・高速データアクセス
├─────────────────────────┤
│ External APIs / Services│ ← 他アプリやサードパーティとの連携
└─────────────────────────┘
```

## ソフトウェアアーキテクチャ
### FEのSWA
```

```


### BEのSWA
```
┌──────────────────────┐
│ Controller / Handler │ ← HTTPリクエスト受け取り
├──────────────────────┤
│ Service / UseCase    │ ← ビジネスロジック
├──────────────────────┤
│ Repository / DAO     │ ← DBや外部APIアクセス
├──────────────────────┤
│ Domain / Entity      │ ← ドメインモデル
└──────────────────────┘
```
**BEのSWA設計における設計項目**
1. WebAPI?REST?マイクロサービス(これはもはやSoftwaredではなくApplicationレイヤーのアーキテクチャなので除外しても良い？
2. レイヤー(構造)
4. API設計
5. 非機能/運用

|設計項目|内容|
|----|----|
|

